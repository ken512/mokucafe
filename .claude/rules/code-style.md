---
paths:
  - "src/**/*.ts"
  - "src/**/*.tsx"
---
# コーディングルール

- コンポーネントは関数コンポーネント（アロー関数）で統一
- any型の使用禁止
- 単一原則に沿ってコンポーネントを作成
- フォームは react-hook-form を使用
- コメントは日本語で記述
- 変数名・関数名はキャメルケース（camelCase）
- Reactコンポーネントファイル名はPascalCaseで始める
- TypeScriptファイル名はキャメルケース（camelCase）で始める